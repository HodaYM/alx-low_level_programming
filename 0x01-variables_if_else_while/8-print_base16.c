#include <stdio.h>
/**
 * * main - Entry point
 *
 * Print all the numbers in lowercase
 *
 * Return:Always = 0 (Success)
 */
int main(void)
{
	int i;
	char c = i + 0;

	for (i = 0; i <= 15; i++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
