#include <stdio.h>
/**
 * main - A program that prints its program name
 * @argc: Arugments counts
 * @argv: Arugments vectors
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);
return (0);
}
