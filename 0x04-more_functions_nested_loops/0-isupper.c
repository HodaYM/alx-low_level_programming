#include "main.h"

/**
 * _isupper - Function that checks for upper character
 *  @c: The number to be checked
 *  Return: 1 for upper letters or 0 if any else
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
